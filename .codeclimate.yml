# Save as .codeclimate.yml (note leading .) in project root directory
engines:
  fixme:
    enabled: true
  pep8:
    enabled: true
  radon:
    enabled: true
ratings:
  paths:
  - "**.py"
exclude_paths:
  - "ci/**/*"
  - "tests/**/*"
  - "docs/**/*"
  - "setup.py"