# Save as .codeclimate.yml (note leading .) in project root directory
engines:
  radon:
    enabled: true
ratings:
  paths:
  - "**.py"
exclude_paths:
  - "ci/**/*"
  - "tests/**/*"
  - "docs/**/*"
  - "setup.py"